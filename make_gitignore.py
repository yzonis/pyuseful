
def onlySubFolders(subFoldersList):
#
# onlySubFolders(subFoldersList) writes a new .gitignore file
# that sets git to ignore everything but specific subfolders
#
# input:
#
#     subFoldersList - a list of strings, each containing
#                      the full path of a required subfolder
#                      to include.
#
# * paths should use slahs and not backslash (/ and not \)
#
# * paths should start with a slash that indicates the git folder's root
#
# * paths should end with a slash as they point to folders and not files
#
